import styles from "./tag.module.css";

const DemoTag = () => {
    return (
        <div className={styles["layout-demo-tag-container"]}>
            <p>This is just a DEMO</p>
        </div>
    );
};

export default DemoTag;
